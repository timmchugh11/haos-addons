import { ExtendedTimer } from './extended_timer';
/**
 * A helper class that performs an async function at the given interval
 * and emits the result on a regular basis
 */
export declare class AsyncTimer<Type = any> extends ExtendedTimer<Type> {
    constructor(func: () => Promise<Type>, interval: number, autostart?: boolean);
    /** @ignore */
    on(event: 'data', listener: (payload: Type, timestamp: number, interval: number) => void): this;
    on(event: any, listener: (...args: any[]) => void): this;
}

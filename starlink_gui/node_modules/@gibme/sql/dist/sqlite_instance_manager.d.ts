import { SQLiteInstance } from './sqlite_instance';
export { SQLiteInstance };
export declare class SQLiteInstanceManager {
    private static instances;
    /**
     * Retrieves a singleton instance of SQLite based upon the supplied filename
     * or, creates a new instance if one does not already exist
     *
     * @param filename
     * @param readonly
     */
    static get(filename: string, readonly?: boolean): Promise<SQLiteInstance>;
}

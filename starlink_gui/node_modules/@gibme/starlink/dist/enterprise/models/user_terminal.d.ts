import BaseAPI, { Starlink } from '../api/base_api';
import Router from './router';
import type { ServiceLine } from './service_line';
export default class UserTerminal extends BaseAPI {
    private userTerminal;
    constructor(client_id: string, client_secret: string, userTerminal: Starlink.Management.Response.UserTerminal);
    /**
     * Adds the terminal to the service line specified
     *
     * @param serviceLineOrNumber
     */
    add_to_service_line(serviceLineOrNumber: ServiceLine | string): Promise<boolean>;
    /**
     * Removes the terminal from it's assigned service line
     */
    remove_from_service_line(): Promise<boolean>;
    /**
     * Converts the instance to a string (JSON encoded)
     */
    toString(): string;
    get object(): Starlink.Management.Response.UserTerminal;
    get accountNumber(): string;
    get active(): boolean;
    get dishSerialNumber(): string;
    get kitSerialNumber(): string;
    get nickname(): string | null;
    get routers(): Router[];
    get serviceLineNumber(): string | null;
    get userTerminalId(): string;
}
export { UserTerminal };

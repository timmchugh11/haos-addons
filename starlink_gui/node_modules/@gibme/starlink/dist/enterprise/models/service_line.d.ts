import BaseAPI, { Starlink } from '../api/base_api';
import type { UserTerminal } from './user_terminal';
export default class ServiceLine extends BaseAPI {
    private serviceLine;
    private nickname_updated;
    private product_updated;
    private public_ip_updated;
    constructor(client_id: string, client_secret: string, serviceLine: Starlink.Management.Response.ServiceLine);
    /**
     * Adds the specified terminal to this service line
     *
     * @param terminalOrId
     */
    add_terminal(terminalOrId: string | UserTerminal): Promise<boolean>;
    /**
     * Fetches all daily data usage by asset for the last 1 month(s) plus the current cycle
     *
     * @param options
     */
    fetch_daily_usage(options?: Partial<{
        /**
         * When true; unknown data bins will be included in results.
         */
        includeUnknownDataBin: boolean;
    }>): Promise<Starlink.Management.Response.ServiceLineDataUsage>;
    /**
     * Fetches all historical partial subscription periods by asset for the last 1 month(s) plus the current cycle.
     * Does not include the incomplete period corresponding to the service line's current product
     */
    fetch_partial_periods(): Promise<{
        periodEnd: Date;
        periodStart: Date;
        productReferenceId: string;
    }[]>;
    /**
     * Opt into product for subscription
     */
    opt_in(): Promise<Starlink.Management.Response.OptInProduct>;
    /**
     * Opt out of product for subscription
     */
    opt_out(): Promise<Starlink.Management.Response.OptInProduct>;
    /**
     * Removes the specified terminal from this service line
     *
     * @param terminalOrId
     */
    remove_terminal(terminalOrId: string | UserTerminal): Promise<boolean>;
    /**
     * Attempts to save any changes to the `nickname`, `productReferenceId`, or `publicIp`
     */
    save(): Promise<boolean>;
    /**
     * Converts the instance to a string (JSON encoded)
     */
    toString(): string;
    get object(): Starlink.Management.Response.ServiceLine;
    get accountNumber(): string;
    get active(): boolean;
    get addressReferenceId(): string;
    get aviationMetadata(): Starlink.Management.Components.AviationMetadata | null;
    get delayedProductId(): string | null;
    get endDate(): Date | null;
    get nickname(): string | null;
    set nickname(value: string);
    get optInProductId(): string | null;
    get productReferenceId(): string;
    set productReferenceId(value: string);
    get publicIp(): boolean;
    set publicIp(value: boolean);
    get serviceLineNumber(): string;
    get startDate(): Date | null;
}
export { ServiceLine };

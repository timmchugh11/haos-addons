import BaseAPI, { Starlink } from '../api/base_api';
export default class Router extends BaseAPI {
    private router;
    constructor(client_id: string, client_secret: string, router: Starlink.Management.Response.Router);
    /**
     * Assign a configuration to the router
     *
     * @param configId
     */
    assign_config(configId: string): Promise<boolean>;
    /**
     * Remove the configuration assignment of the router
     */
    remove_config(): Promise<boolean>;
    /**
     * Converts the instance to a string (JSON encoded)
     */
    toString(): string;
    get accountNumber(): string | null;
    get configId(): string | null;
    get directLinkToDish(): boolean;
    get hardwareVersion(): string;
    get lastBonded(): Date | null;
    get nickname(): string | null;
    get routerId(): string;
    get userTerminalId(): string;
}
export { Router };

import { DeepPartial, DeviceClient, DeviceDefinition } from '../../protobuf/spacex/api/device/service';
import type { Request, Response } from '../../protobuf/spacex/api/device/device';
export default abstract class GRPCApi {
    readonly host: string;
    readonly port: number;
    readonly timeout?: number | undefined;
    protected client: DeviceClient;
    private readonly channel;
    protected constructor(host: string, port: number, timeout?: number | undefined);
    /**
     * Closes the underlying gRPC channel and releases resources
     */
    close(): void;
    /**
     * Performs a handle request against the GRPC server
     *
     * @param request
     * @param timeout
     * @protected
     */
    protected handle(request: DeepPartial<Request>, timeout?: number | undefined): Promise<Response>;
}
export { DeviceClient, DeviceDefinition, GRPCApi };

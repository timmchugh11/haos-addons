import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
export declare const protobufPackage = "SpaceX.API.Device";
export declare enum Capability {
    READ = 0,
    WRITE = 1,
    DEBUG = 2,
    ADMIN = 3,
    SETUP = 4,
    SET_SKU = 5,
    REFRESH = 6,
    READ_PRIVATE = 7,
    UNRECOGNIZED = -1
}
export declare function capabilityFromJSON(object: any): Capability;
export declare function capabilityToJSON(object: Capability): string;
export interface PublicKey {
    key?: string | undefined;
    capabilities: Capability[];
}
export declare const PublicKey: MessageFns<PublicKey>;
type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;
export type DeepPartial<T> = T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>> : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>> : T extends {} ? {
    [K in keyof T]?: DeepPartial<T[K]>;
} : Partial<T>;
export interface MessageFns<T> {
    encode(message: T, writer?: BinaryWriter): BinaryWriter;
    decode(input: BinaryReader | Uint8Array, length?: number): T;
    fromJSON(object: any): T;
    toJSON(message: T): unknown;
    create(base?: DeepPartial<T>): T;
    fromPartial(object: DeepPartial<T>): T;
}
export {};
